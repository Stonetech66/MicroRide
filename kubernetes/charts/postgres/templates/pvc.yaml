apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "postgres-{{ .Values.appName }}"
spec:
  accessModes:
    - {{ .Values.accessMode }}
  storageClassName: {{ .Values.storageClassName }}
  resources:
    requests:
      storage: {{ .Values.appStorage }}